<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_balance_container"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    tools:context=".ui.user.BalanceFragment">

    <ProgressBar
        android:id="@+id/loading"
        style="@style/ParkeerAssistent.Loading"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginTop="@dimen/balance_loading_vertical_margin"
        android:layout_marginBottom="@dimen/balance_loading_vertical_margin"
        android:layout_marginEnd="@dimen/horizontal_margin"
        app:layout_constraintBottom_toBottomOf="@id/border_bottom"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/balance"
        style="@style/ParkeerAssistent.Text"
        android:textColor="@color/primary"
        android:visibility="invisible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/border_bottom"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/border_bottom"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/primary"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>